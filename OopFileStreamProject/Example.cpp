#include <iostream>
#include <fstream>
#include <sstream>

void ExampleTextFile()
{
    setlocale(LC_ALL, "Russian");

    //std::string str = "Данный раздел посвящен низкоуровнему программированию и программированию на ассемблерах.";

    /*std::ofstream ofile("file.txt", std::ios::out);
    ofile << "Hello world";
    ofile.close();*/

    /*std::ofstream ofile2;
    ofile2.open("file.txt", std::ios::out);*/

    /*std::ifstream ifile("file.txt", std::ios::in);
    std::string word;*/

    //ifile >> word;


    /*while (!ifile.eof())
    {
        std::getline(ifile, word);
        std::cout << word << "\n";
    }


    ifile.close();*/

    /*std::fstream file("file.txt", std::ios::app);

    file << "\n" << str;

    file.close();*/
}